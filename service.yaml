apiVersion: v1
kind: Service
metadata:
  name: homecare-backend-service
  labels:
    app: homecare-backend
spec:
  type: ClusterIP
  selector:
    app: homecare-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
